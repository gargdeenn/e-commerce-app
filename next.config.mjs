/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['tecdn.b-cdn.net', 'via.placeholder.com', 'tailwindui.com', 'localhost', 'unsplash.com', 'www.shutterstock.com'],
    },
};

export default nextConfig;
